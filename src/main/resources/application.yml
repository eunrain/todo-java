spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/todo_java
    username: go-eunbi
    password:
    driver-class-name: org.postgresql.Driver

  security:
    user:
      name: admin
      password: admin

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

springdoc:
  swagger-ui:
    path: /swagger-ui.html